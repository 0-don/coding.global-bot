# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================
# These variables are essential for the bot to function

# Discord Bot Token (REQUIRED)
TOKEN=<YOUR DISCORD BOT TOKEN>

# Database Configuration (REQUIRED)
POSTGRES_USER=<USER NAME>
POSTGRES_PASSWORD=<YOUR PASSWORD>
POSTGRES_HOST=localhost
POSTGRES_DB=coding-global-db
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:5432/${POSTGRES_DB}?schema=public

# =============================================================================
# OPTIONAL FEATURES
# =============================================================================
# These variables enable additional bot features when configured

# AI Features (Optional)
GEMINI_API_KEY=<YOUR GEMINI API KEY>
DEEPL=<YOUR DEEPL API KEY>

# Role System (Optional)
# Use explicit role names (comma separated) that exist in your Discord server
HELPER_ROLES=Helper,Senior Helper,Expert Helper
STATUS_ROLES=Verified,VoiceOnly,Jail
MEMBER_ROLES=Member,Contributor
LEVEL_ROLES=Active!,Super Active!,Mega Active!,Giga Active!,Ultra Active!

# Channel Configuration (Optional)
# Use explicit channel names (comma separated) that exist in your Discord server
GENERAL_CHANNELS=general,chat
BOT_CHANNELS=bot-commands,bot-spam
VERIFY_CHANNELS=verify,welcome
VOICE_EVENT_CHANNELS=voice-logs
JOIN_EVENT_CHANNELS=join-leave-logs
MEMBERS_COUNT_CHANNELS=member-count

# Bot Behavior (Optional)
IS_CONSTRAINED_TO_BOT_CHANNEL=false    # Restrict bot commands to bot channels only
SHOULD_LOG_VOICE_EVENTS=false          # Log voice channel join/leave events
SHOULD_COUNT_MEMBERS=false             # Update member count channels automatically
SHOULD_USER_LEVEL_UP=true              # Enable user leveling system

# Appearance (Optional)
BOT_ICON=https://your-bot-icon-url.com/icon.png

# =============================================================================
# CONFIGURATION GUIDE
# =============================================================================
#
# REQUIRED VARIABLES:
# - TOKEN: Get from Discord Developer Portal > Applications > Your Bot > Bot
# - DATABASE_URL: PostgreSQL connection string for data storage
#
# OPTIONAL FEATURES:
# - Leave any optional variable empty or remove it to disable that feature
# - Role/Channel names must match exactly what exists in your Discord server
# - Boolean values: true/false (case sensitive)
#
# EXAMPLES:
# HELPER_ROLES=Junior Helper,Senior Helper,Expert Helper
# STATUS_ROLES=Verified,Muted,Banned
# BOT_CHANNELS=bot-commands,spam
# IS_CONSTRAINED_TO_BOT_CHANNEL=true